This is the same as lab 10, except now instead of pinging the private instance using its private IP address, we now ping it using its private hostname.  This was made possible through DHCP options set change, as well as Route53 private hosting zone and records.