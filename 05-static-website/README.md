This lab creates a static website hosted on S3 but to run the lab, apply the command:
```
terrafrom apply -auto-approve
```

and then run the same command again to resolve the issue with creating the S3 bucket policy.