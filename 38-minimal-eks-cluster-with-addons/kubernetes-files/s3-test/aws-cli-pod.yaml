apiVersion: v1
kind: Pod
metadata:
  name: aws-cli
  namespace: production
spec:
  serviceAccountName: read-s3-service-account
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command: ["sleep", "infinity"]